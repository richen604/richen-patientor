import React from 'react';
import {Entry} from '../types';

interface EntryTypes {
    entry: Entry;
}

const EntryDetails: React.FunctionComponent<EntryTypes> = () => {
    return (
        <div></div>
    );
};

export default EntryDetails;
